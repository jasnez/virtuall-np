/**
 * Main site container: max-width 1200px, horizontal padding 16px (mobile) →
 * 20px (sm) → 24px (md+).
 */
export const PAGE_CONTAINER_X =
  "mx-auto w-full max-w-page px-4 sm:px-5 md:px-6" as const;
